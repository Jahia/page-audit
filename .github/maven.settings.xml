<?xml version="1.0"?>
<!--
  Maven settings used by every jahia-modules-action composite action.
  Every action defaults `mvn_settings_filepath` to this exact path, so the file
  must exist for CI to work. Credentials come from environment variables the
  actions export from the NEXUS_USERNAME / NEXUS_PASSWORD secrets.
-->
<settings>
    <profiles>
        <profile>
            <id>sonar</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <sonar.host.url>${env.SONAR_URL}</sonar.host.url>
                <sonar.login>${env.SONAR_TOKEN}</sonar.login>
            </properties>
        </profile>
    </profiles>
    <servers>
        <server>
            <id>jahia-public</id>
            <username>${env.NEXUS_USERNAME}</username>
            <password>${env.NEXUS_PASSWORD}</password>
        </server>
        <server>
            <id>jahia-snapshots</id>
            <username>${env.NEXUS_USERNAME}</username>
            <password>${env.NEXUS_PASSWORD}</password>
        </server>
        <server>
            <id>jahia-releases</id>
            <username>${env.NEXUS_RELEASE_USERNAME}</username>
            <password>${env.NEXUS_RELEASE_PASSWORD}</password>
        </server>
        <server>
            <id>staging-repository</id>
            <username>${env.NEXUS_USERNAME}</username>
            <password>${env.NEXUS_PASSWORD}</password>
        </server>
    </servers>
    <pluginGroups>
        <pluginGroup>org.owasp</pluginGroup>
    </pluginGroups>
</settings>
